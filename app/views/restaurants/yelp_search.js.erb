var restaurants = [];
<% @restaurants.each do |restaurant| %>
  var current_restaurant = [<%= restaurant.latitude %>, <%= restaurant.longitude %>, '<%= restaurant.name %>'];
  restaurants.push(current_restaurant);
<% end %>
placeRestaurantMarkers(restaurants);
