console.log("You're in the yelp!");
var restaurants = [];
<% @restaurants.each do |restaurant| %>
  var current_restaurant = [<%= restaurant.latitude %>, <%= restaurant.longitude %>];
  restaurants.push(current_restaurant);
<% end %>
placeRestaurantMarkers(restaurants);
